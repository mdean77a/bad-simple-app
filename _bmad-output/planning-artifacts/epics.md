---
stepsCompleted: [step-01-validate-prerequisites, step-02-design-epics, step-03-create-stories]
inputDocuments:
  - _bmad-output/planning-artifacts/prd.md
  - _bmad-output/planning-artifacts/architecture.md
  - _bmad-output/planning-artifacts/ux-design-specification.md
  - _bmad-output/planning-artifacts/validation-report-prd.md
  - _bmad-output/planning-artifacts/prd-validation-report.md
---

# bmad-simple-app - Epic Breakdown

## Overview

This document provides the complete epic and story breakdown for bmad-simple-app, decomposing the requirements from the PRD, UX Design, and Architecture into implementable stories.

## Requirements Inventory

### Functional Requirements

**Authentication & Session Management**
- FR1: User can log in by entering their name and email address
- FR2: User can log out and end their session
- FR3: System identifies and tracks actions by the logged-in user's name

**Protocol Management**
- FR4: Coordinator can upload a clinical protocol in PDF format
- FR5: System can extract text from an uploaded PDF protocol; if extraction fails or PDF is corrupted, system displays a user-facing error message
- FR6: System can chunk, embed, and index protocol content for intelligent retrieval; embeddings are stored in vector database (original PDF is not persistently stored)
- FR7: Coordinator can view a list of protocols that have been indexed in the vector database
- FR8: Coordinator can select a previously indexed protocol for ICF generation

**ICF Outline Management**
- FR9: System can generate a proposed ICF outline from a selected protocol
- FR10: System can detect and include conditional sections (sample storage, genetic research) based on protocol content; detected sections are pre-checked in the outline
- FR11: System can detect participant age ranges and configure appropriate signature pages (adult consent, teen assent, parent permission)
- FR12: Coordinator can review the proposed ICF outline as a checklist before generation
- FR13: Coordinator can check or uncheck sections in the outline checklist to include or exclude them from generation
- FR14: System updates the outline based on coordinator's checklist selections
- FR15: Coordinator can confirm the outline checklist to proceed with section generation

**Section Generation & Review**
- FR16: System can generate all ICF sections in parallel after outline confirmation; generation streams to UI in real-time
- FR17: Coordinator can view all sections on a single page and navigate freely between them; per-section controls (approve, edit, regenerate) are disabled while that section is still generating
- FR18: Coordinator can approve a section as-is
- FR19: Coordinator can directly edit any section content
- FR20: Coordinator can request regeneration of a section
- FR21: Coordinator can provide optional natural language guidance when requesting regeneration; guidance is appended to the original generation prompt
- FR22: System can regenerate a section using relevant protocol content and the original prompt
- FR23: System can regenerate a section incorporating coordinator's guidance appended to the original prompt
- FR24: Coordinator can see the status of each section (generating, ready, editing, edited, approved, error)
- FR24a: If section generation fails (LLM error, network failure, vector DB unreachable), section displays error status with explanation and a Retry button

**Project Management**
- FR25: Coordinator can save an ICF project in progress to a local file (save is disabled while any section is generating)
- FR27: Coordinator can open and resume a previously saved ICF project file
- FR29: Coordinator can start a new ICF project by selecting or uploading a protocol
- FR30: Coordinator can continue a saved project by clicking "Continue Saved Project" and selecting a file from their local system

*Note: FR26 and FR28 were removed - no central project list; users manage project files via file system*

**Approval Tracking**
- FR31: System records approval status for each section (approver identity, date, time); last approver is recorded
- FR32: System records when a previously approved section is edited or re-approved, updating the approver to the current user
- FR33: System tracks the identity of the user who most recently approved each section
- FR34: Coordinator can approve all sections at once ("Approve All" button), which records the current user as approver for all sections
- FR35a: Approval tracking serves as internal audit trail to identify who approved each section if errors are found

**Export & Delivery**
- FR35: Coordinator can export the completed ICF as a PDF document (derived from Markdown)
- FR36: Coordinator can export the completed ICF as a Markdown file (primary export format, generated by LLM)
- FR37: Coordinator can export the completed ICF as a DOCX (Word) file (derived from Markdown)
- FR38: Coordinator can save the exported ICF locally to their computer (any format)
- FR39: Exported ICF includes an approval tracking page as the final page listing each section with its approval date, time, and approver identity

**LLM Provider Management**
- FR44: Coordinator can select an LLM vendor from available providers; deployed environments offer Anthropic and OpenAI; local development environments additionally offer a Local option (LM Studio via ChatOpenAI interface) when enabled via environment variable
- FR45: Coordinator can select a model for the chosen vendor from a per-vendor model list; model selection is hidden when Local vendor is selected (Local uses whatever model LM Studio is serving)
- FR46: Vendor and model selection persists for the session and is included in all generation and regeneration API requests; Anthropic is the default vendor
- FR47: Vendor and model choice is saved in the project file and restored when the project is opened

**User Interface**
- FR40: Application displays correctly on desktop; responsive layout adapts to smaller screens without specific mobile optimization

*Note: FR41-FR43 were removed - desktop-first design with natural Tailwind responsiveness; no platform-specific requirements*

### NonFunctional Requirements

**Performance**
- NFR1: Protocol upload and processing completes within 1 minute
- NFR2: Section generation begins streaming within 10 seconds of request; once streaming starts, text chunks arrive with minimal latency; section completion triggers UI state change
- NFR3: Project save/load operations complete within 5 seconds (local file I/O)
- NFR4: UI interactions (button clicks, navigation, approve/edit actions) respond within 200ms

**Security**
- NFR5: All connections use HTTPS
- NFR6: Protocol index access secured via API key

*Note: NFR7 and NFR8 were removed - no shared persistent storage; projects saved as local files; no server-side session timeout needed*

**Integration**
- NFR9: LLM API requests retry up to 3 times before displaying error to user
- NFR10: Protocol index connection failure prevents section generation; user notified with specific error message
- NFR11: PDF extraction failures display specific error identifying problematic document
- NFR16: System reports available LLM providers based on configured API keys and environment flags; providers without valid configuration are excluded from the list returned to the frontend

**Reliability**
- NFR12: Project state automatically maintained in memory during session (transparent to user)
- NFR13: Users can save project to local file from the dashboard page and resume later by opening that file
- NFR14: Save button and autosave are disabled while any section generation is in progress; no mid-generation state recovery is provided
- NFR15: If connection is lost during generation, user must re-trigger generation for incomplete sections after reconnecting

### Additional Requirements

**From Architecture - Starter Template (CRITICAL for Epic 1 Story 1):**
- Two-tier monorepo structure: frontend (Next.js) + backend (FastAPI)
- Frontend initialization: `npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
- Backend initialization: FastAPI + LangChain 1.0 + LangGraph with uv package manager
- Dependencies: fastapi, uvicorn[standard], python-multipart, langchain>=1.2.0, langgraph>=0.3.0, langchain-anthropic, langchain-openai, langchain-ollama, qdrant-client, pymupdf, python-docx, weasyprint, markdown, pydantic-settings, python-dotenv
- Deployment targets: Vercel (frontend auto-deploy), Render (backend auto-deploy)
- No database required - stateless backend with local file storage

**From Architecture - Data & State:**
- Stateless backend; frontend owns all project state
- Local JSON file storage for projects (no database)
- Client-side user identity (React context/localStorage)
- SSE (Server-Sent Events) for streaming section generation
- REST API with /api/v1/ prefix

**From Architecture - API Endpoints:**
- POST /api/v1/protocols/upload - Upload PDF, extract text, index in Qdrant; returns {protocolId, protocolName}
- GET /api/v1/protocols - List indexed protocols; returns [{protocolId, protocolName, indexedAt}]
- POST /api/v1/outline/generate - Generate outline from protocol ID; returns proposed section checklist
- POST /api/v1/sections/generate - SSE stream section generation; body: {protocolId, sections: [...]}
- POST /api/v1/sections/regenerate - SSE stream section regeneration; body: {protocolId, sectionName, originalPrompt, guidance?}
- POST /api/v1/export - Generate export document; body: {sections: [...], approvals: [...], format: "md"|"docx"|"pdf"}

**From Architecture - Section State Machine:**
- States: generating, ready, editing, edited, approved, error
- Valid transitions:
  - generating → ready, error
  - ready → approved, editing, generating
  - editing → edited, ready (cancel)
  - edited → approved, editing, generating
  - approved → editing, generating
  - error → generating

**From Architecture - Project File Format (JSON):**
- version, protocolId, protocolName, createdAt, lastModifiedAt
- outline: { sections, confirmedAt, confirmedBy }
- sections: [{ id, name, content, status, originalPrompt, approval }]

**From Architecture - Naming Conventions:**
- Python: snake_case for functions/variables, PascalCase for classes, UPPER_SNAKE_CASE for constants
- TypeScript: camelCase for functions/variables, PascalCase for components/types
- JSON/API: camelCase for all fields
- Files: snake_case for Python, camelCase/PascalCase for TypeScript

**From Architecture - LLM Configuration:**
- Environment variables: LLM_PROVIDER (anthropic|openai|ollama), LLM_MODEL, provider API keys
- Qdrant configuration: QDRANT_URL, QDRANT_API_KEY, QDRANT_COLLECTION
- CORS configuration: CORS_ORIGINS for frontend URLs

**From UX Design - Component Requirements:**
- Section Card: primary dashboard component with streaming, status, approve/edit/regenerate actions
- Outline Checklist: checkbox list with LLM pre-selections (Standard Sections, Conditional Sections, Signature Pages)
- Action Bar: sticky bar with "Approve All Sections", "Save Project", and export buttons (PDF/Word/MD)
- Button: Primary (green), Secondary (outline), Destructive (red) variants
- Status Badge: Generating (amber+spinner), Ready (neutral), Approved (green+checkmark), Edited (neutral), Error (red)
- Text Input / TextArea: for login, regenerate guidance
- File Upload: for protocol upload with drag-and-drop
- Spinner: for generating states
- Page Header: with back navigation, title, user name, logout

**From UX Design - Layout:**
- Desktop-first design with max-width container (1280px)
- Natural Tailwind responsiveness handles smaller screens
- No explicit breakpoints or mobile-specific behaviors required

**From UX Design - Color System:**
- Primary: Purple/Violet (violet-600)
- Success: Green (emerald-500) - Approve actions, approved status
- Warning: Amber (amber-500) - Generating status
- Error: Red (red-500) - Error states, destructive actions
- Neutral: Slate - Secondary actions, text, borders
- Export buttons: Red (PDF), Blue (Word), Green (Markdown)

**From UX Design - Accessibility:**
- WCAG 2.1 Level AA best practices (not certified)
- Color contrast 4.5:1 minimum
- Visible focus indicators (focus:ring-2)
- Keyboard navigation: Tab through actions, Enter to activate
- ARIA labels on buttons: "Approve [Section Name]", "Edit [Section Name]", etc.
- aria-live="polite" on status badges for state changes

### FR Coverage Map

| FR | Epic | Description |
|----|------|-------------|
| FR1 | Epic 1 | User login (name + email) |
| FR2 | Epic 1 | User logout |
| FR3 | Epic 1 | System tracks user identity |
| FR40 | Epic 1 | Desktop display with responsive layout |
| FR4 | Epic 2 | Upload clinical protocol PDF |
| FR5 | Epic 2 | Extract text from PDF with error handling |
| FR6 | Epic 2 | Chunk, embed, index protocol content |
| FR7 | Epic 2 | View list of indexed protocols |
| FR8 | Epic 2 | Select protocol for ICF generation |
| FR9 | Epic 3 | Generate proposed ICF outline |
| FR10 | Epic 3 | Detect conditional sections |
| FR11 | Epic 3 | Detect age ranges for signature pages |
| FR12 | Epic 3 | Review outline as checklist |
| FR13 | Epic 3 | Check/uncheck sections in checklist |
| FR14 | Epic 3 | System updates outline based on selections |
| FR15 | Epic 3 | Confirm outline to proceed |
| FR16 | Epic 4 | Generate all sections in parallel with streaming |
| FR17 | Epic 4 | View all sections on single page with status |
| FR24 | Epic 4 | See section status (generating, ready, etc.) |
| FR24a | Epic 4 | Error status with retry button |
| FR18 | Epic 5 | Approve section as-is |
| FR19 | Epic 5 | Edit section content |
| FR20 | Epic 5 | Request section regeneration |
| FR21 | Epic 5 | Provide guidance for regeneration |
| FR22 | Epic 5 | Regenerate with protocol content |
| FR23 | Epic 5 | Regenerate with coordinator guidance |
| FR31 | Epic 6 | Record approval status (user, date, time) |
| FR32 | Epic 6 | Record edits and re-approvals |
| FR33 | Epic 6 | Track most recent approver |
| FR34 | Epic 6 | Approve All button |
| FR35a | Epic 6 | Audit trail for accountability |
| FR25 | Epic 7 | Save project to local file |
| FR27 | Epic 7 | Open and resume saved project |
| FR29 | Epic 7 | Start new project |
| FR30 | Epic 7 | Continue saved project button |
| FR35 | Epic 8 | Export as PDF |
| FR36 | Epic 8 | Export as Markdown |
| FR37 | Epic 8 | Export as Word/DOCX |
| FR38 | Epic 8 | Save export locally |
| FR39 | Epic 8 | Approval tracking page in export |
| FR44 | Epic 9 | Select LLM vendor (Anthropic/OpenAI/Local) |
| FR45 | Epic 9 | Select model for chosen vendor |
| FR46 | Epic 9 | Vendor/model persists for session, included in API requests |
| FR47 | Epic 9 | Vendor/model saved in project file, restored on open |

## Epic List

### Epic 1: Project Foundation & User Access

**Goal:** Developers can run the application locally; users can identify themselves to the system.

**FRs covered:** FR1, FR2, FR3, FR40

**NFRs addressed:** NFR4 (200ms UI response), NFR5 (HTTPS)

**Additional Requirements:**
- Architecture starter template (monorepo, Next.js frontend, FastAPI backend)
- Basic landing page with login form
- User context/localStorage for identity tracking
- Page Header component with logout

---

### Epic 2: Protocol Ingestion Pipeline

**Goal:** Coordinators can upload clinical protocols and have them indexed for ICF generation.

**FRs covered:** FR4, FR5, FR6, FR7, FR8

**NFRs addressed:** NFR1 (1 min processing), NFR6 (API key security), NFR11 (PDF error messages)

**Additional Requirements:**
- POST /api/v1/protocols/upload endpoint
- GET /api/v1/protocols endpoint
- PyMuPDF for PDF extraction
- Qdrant integration for vector storage
- File Upload component with drag-and-drop
- Protocol selection UI

---

### Epic 3: Intelligent Outline Generation

**Goal:** System generates smart ICF outline with conditional sections; coordinators customize via checklist.

**FRs covered:** FR9, FR10, FR11, FR12, FR13, FR14, FR15

**NFRs addressed:** NFR9 (LLM retries)

**Additional Requirements:**
- POST /api/v1/outline/generate endpoint
- LangGraph workflow for outline generation
- Outline Checklist component (Standard, Conditional, Signature sections)
- Confirm button to proceed to generation

---

### Epic 4: Section Generation & Streaming

**Goal:** System generates all ICF sections in parallel with real-time streaming to dashboard.

**FRs covered:** FR16, FR17, FR24, FR24a

**NFRs addressed:** NFR2 (10s streaming start), NFR9 (retries), NFR10 (error messages), NFR15 (reconnection)

**Additional Requirements:**
- POST /api/v1/sections/generate SSE endpoint
- Section Card component with streaming display
- Status Badge component (generating, ready, error states)
- Spinner component
- Error state with Retry button

---

### Epic 5: Section Review Workflow

**Goal:** Coordinators can approve, edit, or regenerate sections with optional guidance.

**FRs covered:** FR18, FR19, FR20, FR21, FR22, FR23

**NFRs addressed:** NFR4 (200ms UI response), NFR9 (LLM retries)

**Additional Requirements:**
- POST /api/v1/sections/regenerate SSE endpoint
- Section state machine (ready → approved, editing, generating)
- Approve/Edit/Regenerate buttons in Section Card
- Regenerate modal with guidance textarea
- Inline content editing

---

### Epic 6: Approval Tracking & Audit

**Goal:** System maintains regulatory accountability audit trail of who approved what and when.

**FRs covered:** FR31, FR32, FR33, FR34, FR35a

**NFRs addressed:** NFR4 (200ms UI response)

**Additional Requirements:**
- Approval data structure (userName, userEmail, timestamp)
- Approve All button in Action Bar
- Approval Badge showing approver and timestamp
- Re-approval tracking when sections edited

---

### Epic 7: Project Persistence

**Goal:** Coordinators can save work-in-progress to local files and resume later.

**FRs covered:** FR25, FR27, FR29, FR30

**NFRs addressed:** NFR3 (5s save/load), NFR12 (memory state), NFR13 (local file), NFR14 (save disabled during gen)

**Additional Requirements:**
- Project file format (JSON with version, sections, approvals)
- Save Project button in Action Bar (disabled during generation)
- Continue Saved Project button on landing page
- File picker for loading projects
- New Project flow (upload or select protocol)

---

### Epic 8: Export & Delivery ✅

**Goal:** Coordinators can export completed ICFs in PDF, Word, or Markdown with approval tracking page.

**FRs covered:** FR35, FR36, FR37, FR38, FR39

**NFRs addressed:** NFR4 (200ms UI response)

**Additional Requirements:**
- POST /api/v1/export endpoint
- Document structure assembled programmatically (title + section headings); no LLM polishing step
- python-docx for Word conversion
- xhtml2pdf for PDF conversion (replaced weasyprint — weasyprint requires system C libraries incompatible with Vercel/Render deployment)
- Export buttons in Action Bar (PDF/Word/Markdown)
- Approval tracking page appended to export with page break separator

---

### Epic 9: Multi-Vendor LLM Support

**Goal:** Coordinators can select between LLM vendors (Anthropic, OpenAI) and models; developers can use a local LLM (LM Studio) during development.

**FRs covered:** FR44, FR45, FR46, FR47

**NFRs addressed:** NFR16 (provider availability reporting)

**Additional Requirements:**
- Expand `llm_factory.py` to support three providers: `"anthropic"` → ChatAnthropic, `"openai"` → ChatOpenAI, `"local"` → ChatOpenAI with base_url override
- Add `enable_local_llm` and `local_llm_base_url` to config.py
- GET /api/v1/settings/providers endpoint (filtered by configured API keys + env flags)
- Generation/regeneration endpoints accept optional `provider`/`model` overrides
- Dashboard settings UI: vendor dropdown + model selector
- Vendor/model stored in project file (`llmProvider`, `llmModel` fields)
- No new dependencies — `langchain-openai` already installed; LM Studio uses ChatOpenAI interface
- Anthropic remains the default vendor

---

## Epic 1: Project Foundation & User Access

**Goal:** Developers can run the application locally; users can identify themselves to the system.

---

### Story 1.1: Initialize Monorepo Project Structure

As a **developer**,
I want **a properly configured monorepo with frontend and backend projects**,
So that **I can run the application locally and begin feature development**.

**Acceptance Criteria:**

**Given** a fresh clone of the repository
**When** I run the frontend setup commands (`cd frontend && npm install && npm run dev`)
**Then** the Next.js application starts on port 3000
**And** I can access http://localhost:3000 in Chrome or Safari

**Given** a fresh clone of the repository
**When** I run the backend setup commands (`cd backend && uv sync && uv run uvicorn src.main:app --reload`)
**Then** the FastAPI application starts on port 8000
**And** I can access the OpenAPI docs at http://localhost:8000/docs

**Given** the backend is running
**When** I make a GET request to `/api/v1/health`
**Then** I receive a 200 response with `{"status": "ok"}`

**Given** the frontend and backend are both running
**When** the frontend makes a request to the backend
**Then** CORS is properly configured and the request succeeds

**Technical Notes:**
- Frontend: `npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
- Backend: FastAPI with uv, pydantic-settings for configuration
- Include `.env.example` files for both projects
- Configure CORS_ORIGINS for localhost:3000

---

### Story 1.2: Create Landing Page with Login Form

As a **research coordinator**,
I want **to enter my name and email to identify myself**,
So that **my actions in the system can be tracked for accountability**.

**Acceptance Criteria:**

**Given** I am not logged in
**When** I navigate to the application root URL
**Then** I see a landing page with a login form containing name and email fields

**Given** I am on the login form
**When** I enter a valid name and email and click "Continue"
**Then** my identity is stored in the browser (localStorage/context)
**And** I see the main landing page with options to start or continue a project

**Given** I am on the login form
**When** I submit the form with an empty name field
**Then** I see a validation error "Name is required"
**And** the form is not submitted

**Given** I am on the login form
**When** I submit the form with an invalid email format
**Then** I see a validation error "Please enter a valid email"
**And** the form is not submitted

**Given** I have previously logged in and closed the browser
**When** I return to the application
**Then** my identity is restored from localStorage
**And** I am taken directly to the main landing page (not the login form)

**Given** I am logged in
**When** the page loads
**Then** UI interactions respond within 200ms (NFR4)

**Technical Notes:**
- Create `src/lib/auth.ts` with React context for user state
- Create `src/components/auth/LoginForm.tsx` component
- Use localStorage key `user` with `{name: string, email: string}`
- Landing page shows: "New Project" button, "Continue Saved Project" button (both disabled for now - enabled in later epics)

---

### Story 1.3: Implement Page Header with User Info and Logout

As a **research coordinator**,
I want **to see my name displayed and be able to log out**,
So that **I know I'm identified correctly and can end my session when done**.

**Acceptance Criteria:**

**Given** I am logged in
**When** I view any page in the application
**Then** I see a Page Header component at the top of the screen
**And** the header displays my name

**Given** I am viewing the Page Header
**When** I click the "Logout" button
**Then** my user data is cleared from localStorage and context
**And** I am redirected to the login form

**Given** I am on a page other than the landing page
**When** I view the Page Header
**Then** I see a back navigation option (where contextually appropriate)

**Given** I am viewing the application on a desktop browser (1024px+)
**When** I look at the Page Header
**Then** the layout displays correctly with user info on the right side

**Given** I am viewing the application on a smaller screen
**When** I look at the Page Header
**Then** the layout adapts naturally using Tailwind's responsive utilities

**Technical Notes:**
- Create `src/components/layout/PageHeader.tsx`
- Props: `title?: string`, `showBack?: boolean`
- Use violet-600 for accent colors per UX spec
- Include accessible keyboard navigation (Tab to logout, Enter to activate)

---

### Story 1.4: Configure and Verify Production Deployment

As a **developer**,
I want **the application deployed to Vercel (frontend) and Render (backend) with working connectivity**,
So that **I can validate the production environment before building features**.

**Acceptance Criteria:**

**Given** I push code to the main branch on GitHub
**When** the push completes
**Then** Vercel automatically deploys the frontend from the `/frontend` directory
**And** Render automatically deploys the backend from the `/backend` directory

**Given** the frontend is deployed to Vercel
**When** I access the Vercel URL in Chrome or Safari
**Then** the application loads successfully over HTTPS (NFR5)

**Given** the backend is deployed to Render
**When** I access the Render URL `/api/v1/health`
**Then** I receive a 200 response with `{"status": "ok"}` over HTTPS (NFR5)

**Given** both frontend and backend are deployed
**When** the frontend makes a request to the backend
**Then** CORS allows the request from the Vercel domain
**And** the request succeeds

**Given** I need to configure the deployment
**When** I set environment variables in Vercel dashboard
**Then** the frontend can read `NEXT_PUBLIC_API_URL` pointing to the Render backend

**Given** I need to configure the backend deployment
**When** I set environment variables in Render dashboard
**Then** the backend reads `CORS_ORIGINS` including the Vercel frontend URL
**And** the backend reads `QDRANT_URL` and `QDRANT_API_KEY` for vector database access (NFR6)

**Technical Notes:**
- Create `vercel.json` in frontend with root directory configuration
- Create `render.yaml` in backend with build and start commands
- Document required environment variables in `.env.example` files
- Production CORS_ORIGINS: `["https://<app-name>.vercel.app"]`
- Render Dockerfile or native Python buildpack

---

### Epic 1 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 1.1 | Initialize Monorepo Project Structure | (Foundation) |
| 1.2 | Create Landing Page with Login Form | FR1, FR3 |
| 1.3 | Implement Page Header with User Info and Logout | FR2, FR40 |
| 1.4 | Configure and Verify Production Deployment | NFR5, NFR6 |

**Total Stories:** 4
**All FRs Covered:** FR1, FR2, FR3, FR40 ✓

---

## Epic 2: Protocol Ingestion Pipeline

**Goal:** Coordinators can upload clinical protocols and have them indexed for ICF generation.

---

### Story 2.1: Implement PDF Upload and Text Extraction

As a **research coordinator**,
I want **to upload a clinical protocol PDF and have text extracted from it**,
So that **the system can process my protocol for ICF generation**.

**Acceptance Criteria:**

**Given** I have a valid clinical protocol PDF
**When** I upload it to `POST /api/v1/protocols/upload`
**Then** the system extracts text from all pages using PyMuPDF
**And** the response includes the extracted text content

**Given** I upload a corrupted or unreadable PDF
**When** the extraction process fails
**Then** I receive a 422 response with error code `PDF_PARSE_ERROR`
**And** the error message identifies the problematic document (NFR11)

**Given** I upload a file that is not a PDF
**When** the system validates the upload
**Then** I receive a 422 response with error code `VALIDATION_ERROR`
**And** the message indicates only PDF files are accepted

**Given** I upload a valid PDF
**When** processing completes
**Then** the total processing time is under 1 minute (NFR1)

**Technical Notes:**

- Create `src/api/routes/protocols.py` with POST `/upload` endpoint
- Create `src/services/pdf_processor.py` using PyMuPDF
- Accept `multipart/form-data` with file field
- Return extracted text for use by Story 2.2

---

### Story 2.2: Implement Protocol Chunking and Vector Indexing

As a **research coordinator**,
I want **my uploaded protocol to be chunked, embedded, and indexed**,
So that **the system can intelligently retrieve relevant sections during ICF generation**.

**Acceptance Criteria:**

**Given** text has been extracted from a PDF (Story 2.1)
**When** the chunking process runs
**Then** the text is split into appropriately sized chunks for embedding

**Given** chunks have been created
**When** the embedding process runs
**Then** each chunk is converted to a vector embedding
**And** embeddings are stored in Qdrant Cloud

**Given** the indexing completes successfully
**When** the upload endpoint returns
**Then** the response includes `protocolId` (Qdrant collection name)
**And** the response includes `protocolName` (derived from filename)

**Given** Qdrant Cloud is unreachable
**When** the system attempts to store embeddings
**Then** I receive a 502 response with error code `VECTOR_DB_ERROR`
**And** a specific error message is displayed (NFR10)

**Given** API key authentication is configured
**When** the system connects to Qdrant
**Then** the connection uses the `QDRANT_API_KEY` for authentication (NFR6)

**Technical Notes:**

- Create `src/services/vector_store.py` for Qdrant operations
- Collection naming: `protocol_{sanitized_filename}_{timestamp}`
- Store protocol name in collection metadata
- Chunk size should balance context retrieval quality
- Use langchain text splitters for chunking

---

### Story 2.3: Create Protocol List Endpoint

As a **research coordinator**,
I want **to see a list of protocols that have been indexed**,
So that **I can select a previously uploaded protocol for ICF generation**.

**Acceptance Criteria:**

**Given** protocols have been indexed in Qdrant
**When** I call `GET /api/v1/protocols`
**Then** I receive a list of all indexed protocols
**And** each item includes `protocolId`, `protocolName`, and `indexedAt`

**Given** no protocols have been indexed
**When** I call `GET /api/v1/protocols`
**Then** I receive an empty array `[]`

**Given** the Qdrant connection is secured
**When** the endpoint retrieves the protocol list
**Then** the request uses API key authentication (NFR6)

**Given** Qdrant Cloud is unreachable
**When** I call `GET /api/v1/protocols`
**Then** I receive a 502 response with error code `VECTOR_DB_ERROR`

**Technical Notes:**

- Add GET `/` route to `src/api/routes/protocols.py`
- Query Qdrant for all collections matching `protocol_*` pattern
- Extract metadata (name, indexedAt) from each collection
- Sort by `indexedAt` descending (most recent first)

---

### Story 2.4: Build Protocol Upload UI

As a **research coordinator**,
I want **a user-friendly interface to upload protocol PDFs**,
So that **I can easily add new protocols to the system**.

**Acceptance Criteria:**

**Given** I am logged in and on the "New Project" page
**When** I view the upload section
**Then** I see a file upload area with drag-and-drop support
**And** I see a button to browse for files

**Given** I drag a PDF file over the upload area
**When** the file is over the drop zone
**Then** the drop zone visually indicates it will accept the file

**Given** I drop or select a PDF file
**When** the upload begins
**Then** I see a progress indicator showing upload status
**And** I see "Processing protocol..." message during extraction/indexing

**Given** the upload and indexing completes successfully
**When** the response is received
**Then** I see a success message with the protocol name
**And** I can proceed to select this protocol for ICF generation

**Given** the upload fails (corrupt PDF, network error)
**When** the error response is received
**Then** I see a specific error message explaining what went wrong
**And** I can try uploading again

**Given** I interact with the upload UI
**When** I click or drag
**Then** the response is within 200ms (NFR4)

**Technical Notes:**

- Create `src/components/projects/ProtocolUpload.tsx`
- Create `src/app/projects/new/page.tsx` for new project flow
- Use native file input with drag-and-drop wrapper
- Call `POST /api/v1/protocols/upload` with FormData
- Show spinner during processing

---

### Story 2.5: Build Protocol Selection UI

As a **research coordinator**,
I want **to select from previously uploaded protocols**,
So that **I can generate an ICF without re-uploading the same protocol**.

**Acceptance Criteria:**

**Given** I am logged in and on the "New Project" page
**When** protocols have been indexed
**Then** I see a dropdown or list showing available protocols
**And** each protocol shows its name and indexing date

**Given** I am viewing the protocol list
**When** I select a protocol from the list
**Then** the protocol is highlighted as selected
**And** a "Continue" button becomes enabled

**Given** I have selected a protocol
**When** I click "Continue"
**Then** I am navigated to the outline review page (placeholder for Epic 3)
**And** the selected `protocolId` is passed to the next step

**Given** no protocols have been indexed
**When** I view the protocol selection area
**Then** I see a message "No protocols uploaded yet"
**And** I am prompted to upload a protocol first

**Given** the protocol list is loading
**When** I view the selection area
**Then** I see a loading skeleton or spinner

**Given** I am on the landing page (logged in)
**When** I click "New Project"
**Then** I am navigated to the new project page with upload and selection options

**Technical Notes:**

- Create `src/components/projects/ProtocolSelect.tsx`
- Call `GET /api/v1/protocols` on page load
- Store selected `protocolId` in component state
- Update landing page "New Project" button to navigate to `/projects/new`
- Format dates using relative time for recent, full date for older

---

### Epic 2 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 2.1 | Implement PDF Upload and Text Extraction | FR4, FR5 |
| 2.2 | Implement Protocol Chunking and Vector Indexing | FR6 |
| 2.3 | Create Protocol List Endpoint | FR7 |
| 2.4 | Build Protocol Upload UI | FR4 |
| 2.5 | Build Protocol Selection UI | FR7, FR8 |

**Total Stories:** 5
**All FRs Covered:** FR4, FR5, FR6, FR7, FR8 ✓
**NFRs Addressed:** NFR1, NFR6, NFR10, NFR11 ✓

---

## Epic 3: Intelligent Outline Generation

**Goal:** System generates smart ICF outline with conditional sections; coordinators customize via checklist.

---

### Story 3.1: Implement Outline Generation Endpoint

As a **research coordinator**,
I want **the system to analyze my protocol and propose an intelligent ICF outline**,
So that **I don't have to manually determine which sections are needed**.

**Acceptance Criteria:**

**Given** I have selected a protocol
**When** I call `POST /api/v1/outline/generate` with the `protocolId`
**Then** the system retrieves relevant protocol content from Qdrant
**And** uses an LLM to analyze the protocol and propose ICF sections

**Given** the outline generation completes
**When** the response is returned
**Then** it includes an array of sections with `sectionName`, `isConditional`, `defaultChecked`, and `detectionReason`

**Given** the protocol mentions genetic sample collection
**When** the outline is generated
**Then** the "Genetic Research" section is included with `defaultChecked: true`
**And** `detectionReason` indicates "detected: genetic sample collection" (FR10)

**Given** the protocol mentions optional sample storage
**When** the outline is generated
**Then** the "Sample Storage" section is included with appropriate `defaultChecked` value (FR10)

**Given** the protocol specifies participant ages including minors (e.g., 16-17)
**When** the outline is generated
**Then** "Teen Assent Signature" page is included with `defaultChecked: true`
**And** `detectionReason` indicates "detected: ages 16-17" (FR11)

**Given** the protocol specifies only adult participants
**When** the outline is generated
**Then** only "Adult Consent Signature" page is included (FR11)

**Given** the LLM request fails
**When** the system retries
**Then** it retries up to 3 times before returning an error (NFR9)

**Given** all retries are exhausted
**When** the final attempt fails
**Then** I receive a 502 response with error code `LLM_ERROR`
**And** a specific error message is displayed

**Technical Notes:**

- Create `src/api/routes/outline.py` with POST `/generate` endpoint
- Create `src/services/rag_pipeline.py` with LangGraph outline workflow
- Standard sections: Purpose, Procedures, Risks, Benefits, Alternatives, Confidentiality, Costs/Compensation, Voluntary Participation, Contact Information
- Conditional sections: Genetic Research, Sample Storage, HIV Testing
- Signature pages: Adult Consent, Teen Assent, Parent/Guardian Permission
- Use structured output from LLM for reliable parsing

---

### Story 3.2: Build Outline Checklist UI

As a **research coordinator**,
I want **to review the proposed outline as an interactive checklist**,
So that **I can include or exclude sections based on my knowledge of the study**.

**Acceptance Criteria:**

**Given** I have selected a protocol and clicked "Continue"
**When** I arrive at the outline review page
**Then** I see the proposed outline displayed as a checklist
**And** sections are grouped by category: Standard Sections, Conditional Sections, Signature Pages (FR12)

**Given** I am viewing the checklist
**When** I look at each section item
**Then** I see a checkbox, section name, and detection reason (if applicable)
**And** checkboxes reflect the `defaultChecked` values from the API

**Given** a conditional section was detected
**When** I view that section
**Then** I see the detection reasoning (e.g., "detected: genetic sample collection")

**Given** I want to include a section that was not pre-checked
**When** I click the checkbox for that section
**Then** the checkbox becomes checked (FR13)

**Given** I want to exclude a pre-checked section
**When** I click the checkbox to uncheck it
**Then** the checkbox becomes unchecked (FR13)

**Given** I make changes to the checklist
**When** I view the outline
**Then** my changes are reflected in the UI state (FR14)

**Given** I interact with checkboxes
**When** I click to toggle
**Then** the response is within 200ms (NFR4)

**Given** I am using keyboard navigation
**When** I press Tab
**Then** I can navigate between checkboxes
**And** Space toggles the focused checkbox

**Technical Notes:**

- Create `src/app/projects/[id]/outline/page.tsx`
- Create `src/components/outline/OutlineChecklist.tsx`
- Create `src/components/outline/OutlineCheckbox.tsx`
- Call `POST /api/v1/outline/generate` on page load
- Store checklist state in React useState
- Group sections using category headers with appropriate styling

---

### Story 3.3: Implement Outline Confirmation Flow

As a **research coordinator**,
I want **to confirm my outline selections and proceed to section generation**,
So that **the system generates only the sections I've chosen**.

**Acceptance Criteria:**

**Given** I am on the outline review page
**When** I have reviewed and adjusted the checklist
**Then** I see an "Confirm Outline" button at the bottom (FR15)

**Given** I click "Confirm Outline"
**When** the confirmation is processed
**Then** the selected sections are stored in project state
**And** I am navigated to the section dashboard page

**Given** outline confirmation succeeds
**When** I arrive at the dashboard
**Then** section generation begins automatically (triggers Epic 4)
**And** the confirmed outline is recorded with `confirmedAt` timestamp and `confirmedBy` user info

**Given** I have not checked any sections
**When** I try to click "Confirm Outline"
**Then** the button is disabled
**And** I see a message indicating at least one section must be selected

**Given** I want to go back and change my protocol selection
**When** I click the back navigation
**Then** I return to the protocol selection page
**And** my outline progress is not saved

**Given** outline generation is still loading
**When** I view the page
**Then** I see a loading state with spinner
**And** the "Confirm Outline" button is disabled

**Technical Notes:**

- Create `src/components/outline/ConfirmButton.tsx`
- Store confirmed outline in project state: `{ sections: string[], confirmedAt: ISO8601, confirmedBy: {name, email} }`
- Generate UUIDs for each section at confirmation time
- Navigation to `/projects/[id]` (dashboard) after confirmation
- Pass section list to dashboard for generation trigger

---

### Story 3.4: Navigate Back to Outline from Dashboard

As a **research coordinator**,
I want **to return to the outline page and modify my section selections**,
So that **I can add forgotten sections or remove unnecessary ones without starting over**.

**Acceptance Criteria:**

**Given** I am on the section dashboard
**When** I look at the page header or navigation area
**Then** I see an "Edit Outline" button or link

**Given** I click "Edit Outline"
**When** the outline page loads
**Then** I see the outline checklist with my current selections preserved
**And** sections that exist on the dashboard are checked
**And** sections not on the dashboard are unchecked

**Given** I am on the outline page after returning from dashboard
**When** I view sections that have been generated
**Then** I see an indicator that these sections already have content (e.g., "Generated" label)

**Given** I want to add a forgotten section
**When** I check a previously unchecked section
**Then** the checkbox becomes checked
**And** the section is marked as "Will be generated" or similar

**Given** I want to remove an existing section
**When** I uncheck a previously checked section
**Then** the checkbox becomes unchecked
**And** the section is marked as "Will be removed" or similar

**Given** I have made changes to the outline
**When** I click "Update Outline"
**Then** I am navigated back to the dashboard
**And** my changes are applied (new sections added, removed sections gone)

**Given** I am on the outline page after returning from dashboard
**When** I click "Cancel" or back navigation
**Then** I return to the dashboard without any changes
**And** all sections remain as they were

**Given** any section is currently generating on the dashboard
**When** I try to navigate to "Edit Outline"
**Then** the action is disabled or I see a message to wait for generation to complete

**Technical Notes:**

- Add "Edit Outline" button to dashboard header or Action Bar
- Pass current section list to outline page via state/context
- Track which sections are "existing" vs "new" vs "removed"
- Disable outline editing while generation is in progress
- Store pending changes until "Update Outline" is clicked

---

### Epic 3 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 3.1 | Implement Outline Generation Endpoint | FR9, FR10, FR11 |
| 3.2 | Build Outline Checklist UI | FR12, FR13, FR14 |
| 3.3 | Implement Outline Confirmation Flow | FR15 |
| 3.4 | Navigate Back to Outline from Dashboard | FR13, FR14 |

**Total Stories:** 4
**All FRs Covered:** FR9, FR10, FR11, FR12, FR13, FR14, FR15 ✓
**NFRs Addressed:** NFR9 ✓

---

## Epic 4: Section Generation & Streaming

**Goal:** System generates all ICF sections in parallel with real-time streaming to dashboard.

**Technical Note:** The streaming implementation (currently specified as SSE) is a candidate for technical review during implementation. Alternative approaches (WebSockets, polling, or different streaming patterns) may prove more optimal depending on infrastructure constraints and LangChain/LangGraph integration patterns.

---

### Story 4.1: Implement Section Generation Streaming Endpoint

As a **research coordinator**,
I want **the system to generate all ICF sections simultaneously with real-time streaming**,
So that **I can see content appearing quickly and track generation progress**.

**Acceptance Criteria:**

**Given** I have confirmed an outline with selected sections
**When** I call `POST /api/v1/sections/generate` with `protocolId` and `sections` array
**Then** the endpoint returns a stream of generation events
**And** all sections begin generating in parallel (FR16)

**Given** section generation begins
**When** the first chunk is ready
**Then** streaming begins within 10 seconds of the request (NFR2)

**Given** sections are generating
**When** content is produced by the LLM
**Then** the stream emits `section_start` events with `sectionId` and `name`
**And** emits `section_chunk` events with `sectionId` and `content` fragments
**And** emits `section_complete` events with `sectionId` and `status: "ready"`

**Given** a section generation fails after retries
**When** the error is confirmed
**Then** the stream emits `section_error` event with `sectionId`, `status: "error"`, and `message`

**Given** the LLM request fails for a section
**When** the system retries
**Then** it retries up to 3 times before emitting error (NFR9)

**Given** the vector database is unreachable
**When** RAG retrieval fails
**Then** a `section_error` event is emitted with specific error message (NFR10)

**Technical Notes:**

- Create `src/api/routes/sections.py` with POST `/generate` endpoint
- Use FastAPI `StreamingResponse` with `text/event-stream` (SSE) or alternative streaming approach
- Extend `src/services/rag_pipeline.py` with section generation workflow
- Each section retrieves relevant protocol chunks from Qdrant
- Store original prompt per section for later regeneration
- Streaming approach may be revised during implementation based on LangGraph integration patterns

---

### Story 4.2: Build Section Dashboard Layout

As a **research coordinator**,
I want **to see all ICF sections on a single page**,
So that **I can review the complete document and track overall progress**.

**Acceptance Criteria:**

**Given** I have confirmed an outline
**When** I arrive at the section dashboard
**Then** I see all confirmed sections displayed in a vertical stack (FR17)
**And** sections appear in the order defined by the outline

**Given** I am viewing the dashboard
**When** I look at the page header
**Then** I see the protocol name and project context

**Given** sections are displayed
**When** I scroll the page
**Then** I can navigate freely between all sections (FR17)

**Given** some sections are generating while others are complete
**When** I view the dashboard
**Then** I can see the status of each section at a glance

**Given** I am viewing on desktop (1024px+)
**When** the dashboard renders
**Then** sections display in a centered container with max-width 1280px
**And** generous whitespace separates each section card

**Given** I am viewing on a smaller screen
**When** the dashboard renders
**Then** the layout adapts naturally using Tailwind responsive utilities

**Technical Notes:**

- Create `src/app/projects/[id]/page.tsx` for dashboard
- Use vertical flex/grid layout for section cards
- Include Page Header with protocol name
- Store dashboard state in React useState/useReducer
- Initialize streaming connection on page mount

---

### Story 4.3: Implement Section Card with Streaming Display

As a **research coordinator**,
I want **to see section content streaming in real-time with clear status indicators**,
So that **I know the system is working and can read content as it's generated**.

**Acceptance Criteria:**

**Given** a section is generating
**When** I view the section card
**Then** I see a Status Badge showing "Generating" with amber color and spinner (FR24)
**And** I see text streaming into the content area as chunks arrive (FR16)

**Given** streaming chunks arrive
**When** content is appended
**Then** the text appears smoothly without jarring updates
**And** the word count updates in real-time

**Given** a section completes generation
**When** the `section_complete` event is received
**Then** the Status Badge changes to "Ready" with neutral color (FR24)
**And** action buttons become enabled (for Epic 5)

**Given** a section is in "generating" status
**When** I view the action area
**Then** the Approve, Edit, and Regenerate buttons are disabled (FR17)

**Given** I am viewing the section card
**When** I look at the header
**Then** I see the section title, status badge, and word count

**Given** content is very long
**When** I view the section card
**Then** content area has a maximum height with scroll capability

**Technical Notes:**

- Create `src/components/dashboard/SectionCard.tsx`
- Create `src/components/dashboard/StreamingContent.tsx`
- Create `src/components/common/StatusBadge.tsx`
- Create `src/components/common/Spinner.tsx`
- Create `src/lib/sse.ts` for stream handling (EventSource or alternative)
- Use `streamingContent` state separate from final `content` during generation
- Status Badge variants: generating (amber + spinner), ready (slate), approved (green + checkmark), error (red)

---

### Story 4.4: Implement Error Handling and Retry

As a **research coordinator**,
I want **to see clear error messages and retry failed sections**,
So that **I can recover from generation failures without starting over**.

**Acceptance Criteria:**

**Given** a section generation fails
**When** the `section_error` event is received
**Then** the Status Badge changes to "Error" with red color (FR24a)
**And** an error message is displayed in the content area

**Given** a section is in error state
**When** I view the section card
**Then** I see a "Retry" button (FR24a)
**And** the error message explains what went wrong

**Given** the error was an LLM failure
**When** I view the error message
**Then** it indicates "Generation failed after multiple attempts"

**Given** the error was a vector database connection failure
**When** I view the error message
**Then** it indicates "Unable to retrieve protocol content" (NFR10)

**Given** a section is in error state
**When** I click the "Retry" button
**Then** the section status changes to "Generating"
**And** generation is re-attempted for that section only

**Given** connection is lost during generation
**When** I reconnect to the application
**Then** sections that were generating show as incomplete
**And** I can trigger regeneration for incomplete sections (NFR15)

**Given** multiple sections fail
**When** I view the dashboard
**Then** each failed section shows its own error and Retry button independently

**Technical Notes:**

- Add error state handling to `SectionCard.tsx`
- Create retry mechanism that calls `/api/v1/sections/regenerate` for single section
- Store error message in section state
- Handle stream connection drops gracefully
- On reconnect, check section states and show appropriate UI

---

### Story 4.5: Handle Outline Modifications

As a **research coordinator**,
I want **my dashboard to update correctly when I modify the outline**,
So that **new sections are generated and removed sections disappear without affecting my other work**.

**Acceptance Criteria:**

**Given** I return from the outline page with a newly checked section
**When** the dashboard loads
**Then** the new section appears in the correct position (based on outline order)
**And** the new section shows "Generating" status
**And** generation begins automatically for the new section only

**Given** I return from the outline page with a newly checked section
**When** generation completes for the new section
**Then** the section shows "Ready" status
**And** I can approve, edit, or regenerate it like any other section

**Given** existing sections are on the dashboard
**When** I return from outline with a new section added
**Then** all existing sections retain their content unchanged
**And** all existing sections retain their status (ready, edited, approved)
**And** all existing approvals are preserved

**Given** I return from the outline page with an unchecked section that was NOT approved
**When** the dashboard loads
**Then** the unchecked section is removed from the dashboard
**And** no confirmation is required

**Given** I return from the outline page with an unchecked section that WAS approved
**When** the change is being applied
**Then** I see a confirmation dialog: "Remove '[Section Name]'? This section has been approved and will be deleted."
**And** I can confirm to remove or cancel to keep it

**Given** I confirm removal of an approved section
**When** the dashboard updates
**Then** the section is removed from the dashboard
**And** the progress indicator updates (e.g., "7/11 approved" instead of "8/12")

**Given** I cancel removal of an approved section
**When** the dialog closes
**Then** the section remains on the dashboard unchanged
**And** the outline is updated to keep that section checked

**Given** multiple sections are added and removed
**When** I return to the dashboard
**Then** all additions and removals are processed correctly
**And** existing unchanged sections are not affected

**Given** I add a new section
**When** I view the Action Bar
**Then** the progress indicator updates to reflect the new total (e.g., "8/13 approved")
**And** export buttons are disabled (not all sections approved)

**Given** sections are reordered due to outline changes
**When** the dashboard renders
**Then** sections display in the order defined by the updated outline

**Technical Notes:**

- Compare previous section list with updated outline to determine adds/removes
- For added sections: create new section entry with "generating" status, trigger generation
- For removed sections: filter out from state (with confirmation if approved)
- Preserve section data (content, status, approval) for unchanged sections by matching section ID or name
- Update progress calculations after modifications
- Re-trigger generation only for new sections, not existing ones

---

### Epic 4 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 4.1 | Implement Section Generation Streaming Endpoint | FR16 |
| 4.2 | Build Section Dashboard Layout | FR17 |
| 4.3 | Implement Section Card with Streaming Display | FR16, FR24 |
| 4.4 | Implement Error Handling and Retry | FR24a |
| 4.5 | Handle Outline Modifications | FR14, FR16 |

**Total Stories:** 5
**All FRs Covered:** FR14, FR16, FR17, FR24, FR24a ✓
**NFRs Addressed:** NFR2, NFR9, NFR10, NFR15 ✓

---

## Epic 5: Section Review Workflow

**Goal:** Coordinators can approve, edit, or regenerate sections with optional guidance.

---

### Story 5.1: Implement Section Approve Action

As a **research coordinator**,
I want **to approve a section with one click**,
So that **I can quickly confirm sections that are ready and track my progress**.

**Acceptance Criteria:**

**Given** a section is in "ready" or "edited" status
**When** I view the section card
**Then** I see an "Approve" button styled green (primary action) (FR18)

**Given** I click the "Approve" button
**When** the action is processed
**Then** the section status changes to "approved" within 200ms (NFR4)
**And** the Status Badge shows green with a checkmark
**And** the approval is recorded with my user info and timestamp

**Given** a section is approved
**When** I view the section card
**Then** the "Approve" button is no longer shown (or shows "Approved" state)
**And** "Edit" and "Regenerate" buttons remain available

**Given** a section is approved
**When** I decide I want to make changes
**Then** I can still click "Edit" or "Regenerate" to modify the section

**Given** I approve a section
**When** the approval is recorded
**Then** the approval data includes `userName`, `userEmail`, and `timestamp` (ISO 8601)

**Given** I am using keyboard navigation
**When** I focus on the Approve button and press Enter
**Then** the section is approved

**Technical Notes:**

- Add approve handler to `SectionCard.tsx`
- Update section state machine: ready → approved, edited → approved
- Store approval in section state: `{ userName, userEmail, timestamp }`
- Get user info from auth context
- Approval data structure prepares for Epic 6 tracking

---

### Story 5.2: Implement Inline Section Editing

As a **research coordinator**,
I want **to directly edit section content**,
So that **I can make corrections or adjustments without regenerating the entire section**.

**Acceptance Criteria:**

**Given** a section is in "ready", "edited", or "approved" status
**When** I view the section card
**Then** I see an "Edit" button (FR19)

**Given** I click the "Edit" button
**When** edit mode activates
**Then** the section content becomes an editable textarea
**And** the Status Badge changes to "Editing"
**And** "Save" and "Cancel" buttons replace the action buttons

**Given** I am in edit mode
**When** I modify the text content
**Then** my changes are reflected in the textarea in real-time

**Given** I have made edits
**When** I click "Save"
**Then** my changes are saved to the section content
**And** the status changes to "edited"
**And** the word count updates to reflect new content
**And** I exit edit mode

**Given** I am in edit mode
**When** I click "Cancel"
**Then** my changes are discarded
**And** the original content is restored
**And** I exit edit mode with the previous status

**Given** I save edits to a previously approved section
**When** the save completes
**Then** the section status changes to "edited" (no longer "approved")
**And** I need to re-approve the section

**Given** I interact with edit controls
**When** I click Save or Cancel
**Then** the response is within 200ms (NFR4)

**Technical Notes:**

- Add editing state and handlers to `SectionCard.tsx`
- Create `src/components/dashboard/SectionContent.tsx` for editable content area
- Use textarea with proper sizing (auto-grow or fixed with scroll)
- State transitions: ready → editing → edited, approved → editing → edited
- Preserve original content for cancel functionality

---

### Story 5.3: Implement Section Regeneration Endpoint

As a **research coordinator**,
I want **the system to regenerate a section using the protocol and optional guidance**,
So that **I can get improved content without manual rewriting**.

**Acceptance Criteria:**

**Given** I request regeneration for a section
**When** I call `POST /api/v1/sections/regenerate` with `protocolId`, `sectionName`, and `originalPrompt`
**Then** the system retrieves relevant protocol content from Qdrant
**And** regenerates the section using the original prompt (FR22)

**Given** I provide optional guidance text
**When** the regeneration request includes `guidance`
**Then** the guidance is appended to the original prompt (FR23)
**And** the LLM uses both to generate improved content

**Given** regeneration begins
**When** content is produced
**Then** the response streams back to the frontend
**And** the new content replaces the previous content

**Given** regeneration completes successfully
**When** the stream ends
**Then** the section status is "ready" (not approved)
**And** the section must be reviewed and approved again

**Given** the LLM request fails
**When** the system retries
**Then** it retries up to 3 times before returning an error (NFR9)

**Given** all retries are exhausted
**When** the final attempt fails
**Then** the stream emits an error event
**And** the section shows error status with the previous content preserved

**Technical Notes:**

- Add POST `/regenerate` route to `src/api/routes/sections.py`
- Request body: `{ protocolId, sectionName, originalPrompt, guidance?: string }`
- Reuse RAG pipeline from Story 4.1 for single-section generation
- Stream response using same event format as bulk generation
- Preserve `originalPrompt` for future regenerations (guidance is one-time)

---

### Story 5.4: Build Regenerate Modal with Guidance Input

As a **research coordinator**,
I want **to provide optional guidance when regenerating a section**,
So that **I can steer the AI to produce better content without prompt engineering skills**.

**Acceptance Criteria:**

**Given** a section is in "ready", "edited", or "approved" status
**When** I click the "Regenerate" button
**Then** a modal dialog opens (FR20)

**Given** the modal is open
**When** I view its contents
**Then** I see the section name in the title
**And** I see a textarea for entering guidance
**And** I see helper text explaining the purpose
**And** I see example guidance phrases as hints (FR21)

**Given** I want to regenerate without guidance
**When** I leave the textarea empty and click "Regenerate"
**Then** the section regenerates using only the original prompt and protocol content

**Given** I want to provide guidance
**When** I enter text like "Make this more reassuring" and click "Regenerate"
**Then** the guidance is sent with the regeneration request (FR21)
**And** the modal closes
**And** the section shows "Generating" status with streaming content

**Given** the modal is open
**When** I click "Cancel" or the X button or press Escape
**Then** the modal closes without triggering regeneration

**Given** the modal is open
**When** I click outside the modal
**Then** the modal closes without triggering regeneration

**Given** I trigger regeneration from the modal
**When** regeneration starts
**Then** the modal closes immediately
**And** I can see the section streaming new content on the dashboard

**Given** I am using keyboard navigation
**When** the modal opens
**Then** focus is trapped inside the modal
**And** Tab navigates between textarea and buttons
**And** Escape closes the modal

**Technical Notes:**

- Create `src/components/dashboard/RegenerateModal.tsx`
- Modal props: `sectionId`, `sectionName`, `onRegenerate`, `onClose`
- Example hints: "Make this more reassuring", "Add the injection site reactions from page 34", "Simpler language for patients"
- Use centered overlay with backdrop
- Call Story 5.3 endpoint on submit
- Accessible: `role="dialog"`, `aria-modal="true"`, focus management

---

### Epic 5 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 5.1 | Implement Section Approve Action | FR18 |
| 5.2 | Implement Inline Section Editing | FR19 |
| 5.3 | Implement Section Regeneration Endpoint | FR22, FR23 |
| 5.4 | Build Regenerate Modal with Guidance Input | FR20, FR21 |

**Total Stories:** 4
**All FRs Covered:** FR18, FR19, FR20, FR21, FR22, FR23 ✓
**NFRs Addressed:** NFR4, NFR9 ✓

---

## Epic 6: Approval Tracking & Audit

**Goal:** System maintains regulatory accountability audit trail of who approved what and when.

---

### Story 6.1: Display Approval Information on Section Cards

As a **research coordinator**,
I want **to see who approved each section and when**,
So that **I have clear accountability records for regulatory purposes**.

**Acceptance Criteria:**

**Given** a section has been approved
**When** I view the section card
**Then** I see an Approval Badge showing the approver's name (FR33)
**And** I see the approval timestamp formatted as "Feb 3, 2026 at 2:30 PM"

**Given** a section has been approved
**When** I view the Approval Badge
**Then** it displays below the Status Badge or in the card header area
**And** it is visually distinct but not overwhelming

**Given** a section has not been approved
**When** I view the section card
**Then** no Approval Badge is displayed

**Given** I am viewing an approved section
**When** I look at the approval information
**Then** I can identify the most recent approver (FR31)

**Given** multiple users work on the same project
**When** different users approve different sections
**Then** each section shows its own approver correctly

**Given** I am on the dashboard
**When** I scan all section cards
**Then** I can quickly see which sections are approved and by whom

**Technical Notes:**

- Create `src/components/dashboard/ApprovalBadge.tsx`
- Props: `userName`, `timestamp`
- Format timestamp using date utility: "MMM D, YYYY at h:mm A"
- Integrate into `SectionCard.tsx` header area
- Only render when section has approval data

---

### Story 6.2: Track Re-approval After Edits

As a **research coordinator**,
I want **approval to be cleared when I edit an approved section**,
So that **edits are always reviewed before the document is finalized**.

**Acceptance Criteria:**

**Given** a section is approved
**When** I click "Edit" and make changes
**Then** the section status changes to "editing"
**And** when I save, the status becomes "edited" (not "approved")

**Given** a section was previously approved by User A
**When** I (User B) edit and then re-approve the section
**Then** the approval record is updated to show User B as the approver (FR32)
**And** the timestamp is updated to the re-approval time

**Given** a section is approved
**When** I trigger regeneration
**Then** the approval is cleared
**And** the regenerated section must be approved again

**Given** a section was previously approved
**When** I view it after editing (before re-approval)
**Then** the Approval Badge is not displayed
**And** the section shows "edited" status requiring approval

**Given** I re-approve an edited section
**When** the approval is recorded
**Then** only the most recent approver is tracked (FR33)
**And** previous approval history is replaced, not appended

**Technical Notes:**

- Clear approval data when transitioning to "editing" or "generating" state
- Update approval data on approve action with current user
- Approval structure: `{ userName, userEmail, timestamp }` (single record, not array)
- State machine handles approval clearing automatically

---

### Story 6.3: Implement Approve All Sections Button

As a **research coordinator**,
I want **to approve all ready sections at once**,
So that **I can quickly finalize the document after reviewing all content**.

**Acceptance Criteria:**

**Given** I am on the dashboard with multiple sections
**When** one or more sections are in "ready" or "edited" status
**Then** I see an "Approve All Sections" button in the Action Bar (FR34)

**Given** I click "Approve All Sections"
**When** the action is processed
**Then** all sections in "ready" or "edited" status are approved
**And** each section records me as the approver with current timestamp (FR34)
**And** sections already "approved" remain unchanged
**And** sections in "generating" or "error" status are skipped

**Given** I click "Approve All Sections"
**When** approvals are applied
**Then** all affected sections update to show "Approved" status within 200ms (NFR4)
**And** Approval Badges appear on each newly approved section

**Given** all sections are already approved
**When** I view the Action Bar
**Then** the button shows "All Approved ✓" or is hidden

**Given** sections are still generating
**When** I view the Action Bar
**Then** the "Approve All Sections" button is disabled

**Given** I am using keyboard navigation
**When** I focus on the Approve All button and press Enter
**Then** all eligible sections are approved

**Technical Notes:**

- Add "Approve All" button to Action Bar component
- Filter sections by status: only approve "ready" and "edited"
- Apply approval with current user to each eligible section
- Button states: enabled (green), disabled (gray), complete (checkmark)

---

### Story 6.4: Build Action Bar with Progress Display

As a **research coordinator**,
I want **to see my overall progress and have quick access to bulk actions**,
So that **I know how close I am to completing the document**.

**Acceptance Criteria:**

**Given** I am on the section dashboard
**When** I view the top of the page
**Then** I see an Action Bar that stays visible while scrolling (sticky)

**Given** sections exist on the dashboard
**When** I view the Action Bar
**Then** I see a progress indicator showing "X/Y approved" (e.g., "8/12 approved") (FR35a)

**Given** some sections are still generating
**When** I view the progress indicator
**Then** it shows "Generating..." or similar status instead of count

**Given** I have approved some sections
**When** I approve more sections
**Then** the progress indicator updates immediately

**Given** all sections are approved
**When** I view the progress indicator
**Then** it shows "12/12 approved ✓" with visual celebration (checkmark, green color)

**Given** I am on the dashboard
**When** I view the Action Bar
**Then** I see export buttons (PDF, Word, Markdown) with proper styling
**And** I see a "Save Project" button

**Given** NOT all sections are approved
**When** I view the export buttons
**Then** they appear disabled (grayed out, cursor not-allowed)

**Given** all sections are approved
**When** I view the export buttons
**Then** they appear enabled with proper colors (PDF=red, Word=blue, Markdown=green)
**And** clicking them has no effect yet (functionality added in Epic 8)

**Given** any section is currently generating
**When** I view the "Save Project" button
**Then** it appears disabled (grayed out)

**Given** no sections are generating (all ready, edited, approved, or error)
**When** I view the "Save Project" button
**Then** it appears enabled
**And** clicking it has no effect yet (functionality added in Epic 7)

**Given** I scroll down the dashboard
**When** the Action Bar would leave the viewport
**Then** it remains sticky at the top with a subtle shadow

**Given** I am viewing on a smaller screen
**When** the Action Bar renders
**Then** it adapts layout appropriately (may stack buttons)

**Technical Notes:**

- Create `src/components/dashboard/ActionBar.tsx`
- Use `sticky top-0` positioning with `z-index` for layering
- Calculate progress from section states: count where status === "approved"
- Export buttons: enable when all sections approved, style by format
- Save button: enable when `!isAnyGenerating`
- onClick handlers are no-ops for now; Epic 7 and 8 will wire them up
- Add shadow on scroll using intersection observer or scroll listener

---

### Epic 6 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 6.1 | Display Approval Information on Section Cards | FR31, FR33 |
| 6.2 | Track Re-approval After Edits | FR32 |
| 6.3 | Implement Approve All Sections Button | FR34 |
| 6.4 | Build Action Bar with Progress Display | FR35a |

**Total Stories:** 4
**All FRs Covered:** FR31, FR32, FR33, FR34, FR35a ✓
**NFRs Addressed:** NFR4 ✓

---

## Epic 7: Project Persistence

**Goal:** Coordinators can save work-in-progress to local files and resume later.

---

### Story 7.1: Define Project File Format and State Structure

As a **developer**,
I want **a well-defined project file format**,
So that **projects can be saved, loaded, and shared reliably**.

**Acceptance Criteria:**

**Given** a project needs to be saved
**When** the file format is used
**Then** it includes a `version` field for compatibility checking

**Given** a project file is created
**When** I examine its structure
**Then** it contains `protocolId` and `protocolName` to identify the source protocol
**And** it contains `createdAt` and `lastModifiedAt` timestamps (ISO 8601)

**Given** a project has a confirmed outline
**When** the file is saved
**Then** it contains `outline` object with `sections` array, `confirmedAt`, and `confirmedBy`

**Given** a project has generated sections
**When** the file is saved
**Then** it contains `sections` array with each section's `id`, `name`, `content`, `status`, `originalPrompt`
**And** approved sections include `approval` object with `userName`, `userEmail`, `timestamp`

**Given** a section is in "generating" or "error" status
**When** the project is saved
**Then** the section is saved with status "ready" (transient states not persisted)
**And** partial content is preserved

**Given** future versions add new fields
**When** an older version loads a newer file
**Then** unknown fields are preserved on save (forward compatibility)

**Given** I need to validate a project file
**When** I check its structure
**Then** required fields are enforced: `version`, `protocolId`, `sections`

**Technical Notes:**

- Create TypeScript interface in `src/types/project.ts`
- Create validation function for loaded files
- Current version: `"1.0"`
- File extension: `.json` (considered `.icfproj` but JSON is more universal)
- Project file schema:

```typescript
interface ProjectFile {
  version: string;
  protocolId: string;
  protocolName: string;
  createdAt: string;
  lastModifiedAt: string;
  outline: {
    sections: string[];
    confirmedAt: string;
    confirmedBy: { name: string; email: string };
  };
  sections: Array<{
    id: string;
    name: string;
    content: string;
    status: "ready" | "edited" | "approved";
    originalPrompt: string;
    approval?: {
      userName: string;
      userEmail: string;
      timestamp: string;
    };
  }>;
}
```

---

### Story 7.2: Implement Save Project to Local File

As a **research coordinator**,
I want **to save my work-in-progress to a local file**,
So that **I can resume later or share with colleagues**.

**Acceptance Criteria:**

**Given** I am on the dashboard with project state
**When** I click the "Save Project" button in the Action Bar
**Then** the project state is serialized to JSON
**And** a file download is triggered with filename `{protocolName}_ICF.json` (FR25)

**Given** I save a project
**When** the file is downloaded
**Then** the save completes within 5 seconds (NFR3)

**Given** any section is currently generating
**When** I view the Save Project button
**Then** it is disabled and cannot be clicked (NFR14)

**Given** all sections have completed (ready, edited, approved, or error)
**When** I view the Save Project button
**Then** it is enabled and clickable

**Given** I save a project
**When** the save completes
**Then** the `lastModifiedAt` timestamp is updated in the saved file

**Given** I have made changes since the last save
**When** I click Save Project
**Then** the new file includes all current section content and approval states

**Given** I save a project multiple times
**When** each save occurs
**Then** the browser downloads a new file (does not overwrite previous automatically)

**Given** sections are in transient states (generating, editing, error)
**When** the project is saved
**Then** those sections are saved as "ready" with their current content preserved

**Technical Notes:**

- Wire up onClick handler in `ActionBar.tsx` Save Project button
- Create `src/lib/projectFile.ts` with `serializeProject()` function
- Use `URL.createObjectURL()` and anchor download pattern for file save
- Sanitize protocol name for filename (remove special characters)
- Map transient states to "ready" on save

---

### Story 7.3: Implement Load Project from Local File

As a **research coordinator**,
I want **to open a previously saved project file**,
So that **I can resume my work where I left off**.

**Acceptance Criteria:**

**Given** I am on the landing page
**When** I click "Continue Saved Project"
**Then** a file picker dialog opens allowing me to select a JSON file (FR30)

**Given** I select a valid project file
**When** the file is parsed
**Then** the project state is restored within 5 seconds (NFR3)
**And** I am navigated to the dashboard with all sections displayed (FR27)

**Given** a loaded project has sections in various states
**When** the dashboard loads
**Then** each section displays its saved content and status
**And** approved sections show their Approval Badges
**And** the Action Bar shows correct progress

**Given** I select an invalid file (not JSON, wrong structure)
**When** parsing fails
**Then** I see an error message "Invalid project file"
**And** I remain on the landing page

**Given** I select a file with an unrecognized version
**When** the file is loaded
**Then** I see a warning "This file was created with a newer version"
**And** the file is loaded anyway (best effort)

**Given** I cancel the file picker
**When** the dialog closes
**Then** I remain on the landing page with no changes

**Given** a loaded project references a protocol
**When** the dashboard loads
**Then** the protocol name is displayed in the header
**And** the `protocolId` is available for regeneration requests

**Technical Notes:**

- Create file input with `accept=".json"` on landing page
- Create `src/lib/projectFile.ts` with `parseProject()` and `validateProject()` functions
- Handle FileReader async loading
- Restore state to React context/state
- Navigate to `/projects/[id]` where id can be generated or derived from file

---

### Story 7.4: Wire Up Landing Page Project Actions

As a **research coordinator**,
I want **clear options to start new work or continue existing work**,
So that **I can quickly get into my workflow**.

**Acceptance Criteria:**

**Given** I am logged in
**When** I view the landing page
**Then** I see a "New Project" button (FR29)
**And** I see a "Continue Saved Project" button (FR30)

**Given** I click "New Project"
**When** the navigation occurs
**Then** I am taken to the protocol selection/upload page
**And** I can upload a new protocol or select an existing one

**Given** I click "Continue Saved Project"
**When** the file picker opens
**Then** I can browse my local file system for project files

**Given** I load a project that was at the outline stage
**When** the project loads
**Then** I am navigated to the outline review page with my previous selections

**Given** I load a project that was at the dashboard stage
**When** the project loads
**Then** I am navigated directly to the dashboard with sections restored

**Given** I load a project where all sections are approved
**When** the dashboard loads
**Then** export buttons are enabled (ready for Epic 8)

**Given** I am on any page in the application
**When** I have a project loaded in memory
**Then** the project state is maintained during navigation (NFR12)

**Technical Notes:**

- Update landing page (`src/app/page.tsx`) with both action buttons
- "New Project" links to `/projects/new`
- "Continue Saved Project" triggers hidden file input
- Determine navigation target based on loaded project state:
  - No outline confirmed → outline page
  - Outline confirmed → dashboard
- Store project state in React context for cross-page persistence

---

### Epic 7 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 7.1 | Define Project File Format and State Structure | FR25 |
| 7.2 | Implement Save Project to Local File | FR25 |
| 7.3 | Implement Load Project from Local File | FR27, FR30 |
| 7.4 | Wire Up Landing Page Project Actions | FR29, FR30 |

**Total Stories:** 4
**All FRs Covered:** FR25, FR27, FR29, FR30 ✓
**NFRs Addressed:** NFR3, NFR12, NFR13, NFR14 ✓

---

## Epic 8: Export & Delivery

**Goal:** Coordinators can export completed ICFs in PDF, Word, or Markdown with approval tracking page.

---

### Story 8.1: Implement Export Endpoint with Format Conversion

As a **research coordinator**,
I want **the system to generate my ICF in PDF, Word, or Markdown format**,
So that **I can deliver the document to the PI in their preferred format**.

**Acceptance Criteria:**

**Given** I have all sections approved
**When** I call `POST /api/v1/export` with sections, approvals, and `format: "md"`
**Then** the system assembles sections into a complete Markdown document (FR36)
**And** returns the Markdown file for download

**Given** I request PDF format
**When** I call the export endpoint with `format: "pdf"`
**Then** the system converts the Markdown to PDF using weasyprint (FR35)
**And** returns the PDF file for download

**Given** I request Word format
**When** I call the export endpoint with `format: "docx"`
**Then** the system converts the Markdown to DOCX using python-docx (FR37)
**And** returns the Word file for download

**Given** the export request is processed
**When** sections are assembled
**Then** sections appear in the order defined by the outline
**And** each section has its title as a heading

**Given** section content contains Markdown formatting
**When** the document is generated
**Then** formatting (paragraphs, lists, emphasis) is preserved in all output formats

**Given** the LLM is used to format the document
**When** formatting occurs
**Then** it adds appropriate document structure (title page, headers, page breaks)

**Given** the export request fails
**When** an error occurs
**Then** I receive a structured error response with code and message

**Technical Notes:**

- Create `src/api/routes/export.py` with POST `/` endpoint
- Create `src/services/export_service.py`
- Request body: `{ sections: [...], approvals: [...], format: "md" | "pdf" | "docx", protocolName: string }`
- Use LLM to create polished Markdown with document structure
- weasyprint for Markdown → PDF (may need CSS for styling)
- python-docx for Markdown → DOCX (use markdown parser to build document)
- Return file with appropriate Content-Type and Content-Disposition headers

---

### Story 8.2: Generate Approval Tracking Page

As a **research coordinator**,
I want **an approval tracking page included in my exported ICF**,
So that **there's a clear audit trail of who approved each section**.

**Acceptance Criteria:**

**Given** I export an ICF
**When** the document is generated
**Then** the final page is an "Approval Tracking" page (FR39)

**Given** the approval tracking page is generated
**When** I view its contents
**Then** I see a table or list of all sections
**And** each section shows: section name, approver name, approval date and time

**Given** a section was approved
**When** the tracking page is generated
**Then** the approval shows formatted as "Feb 3, 2026 at 2:30 PM"
**And** the approver name is displayed (e.g., "Sarah Johnson")

**Given** multiple sections were approved by different users
**When** the tracking page is generated
**Then** each section shows its own approver correctly

**Given** I export as PDF
**When** the tracking page is rendered
**Then** it appears as the last page of the PDF document

**Given** I export as Word
**When** the tracking page is rendered
**Then** it appears as the last section of the Word document

**Given** I export as Markdown
**When** the tracking page is rendered
**Then** it appears as a final section with a table in Markdown format

**Technical Notes:**

- Add approval tracking generation to `export_service.py`
- Format as Markdown table that converts well to PDF/DOCX
- Structure:

```markdown
## Approval Tracking

| Section | Approved By | Date & Time |
|---------|-------------|-------------|
| Purpose of the Study | Sarah Johnson | Feb 3, 2026 at 2:30 PM |
| Study Procedures | Sarah Johnson | Feb 3, 2026 at 2:35 PM |
| Risks and Discomforts | Maria Chen | Feb 4, 2026 at 10:15 AM |
...
```

- Include page break before tracking section in PDF/DOCX

---

### Story 8.3: Wire Up Export Buttons and Download

As a **research coordinator**,
I want **to click export buttons and download my ICF**,
So that **I can quickly get the document I need**.

**Acceptance Criteria:**

**Given** all sections are approved
**When** I view the Action Bar
**Then** the export buttons (PDF, Word, Markdown) are enabled
**And** they display with proper colors (PDF=red, Word=blue, Markdown=green)

**Given** I click the PDF export button
**When** the export is processed
**Then** I see a loading indicator on the button
**And** a PDF file downloads to my computer with filename `{protocolName}_ICF.pdf` (FR38)

**Given** I click the Word export button
**When** the export is processed
**Then** a DOCX file downloads with filename `{protocolName}_ICF.docx` (FR38)

**Given** I click the Markdown export button
**When** the export is processed
**Then** a Markdown file downloads with filename `{protocolName}_ICF.md` (FR38)

**Given** I click an export button
**When** the request is sent to the backend
**Then** the button shows loading state (spinner or disabled)
**And** other export buttons remain clickable

**Given** the export completes successfully
**When** the file downloads
**Then** a success toast appears "Downloaded {filename}"
**And** the button returns to normal state

**Given** the export fails
**When** the error is received
**Then** an error toast appears with the error message
**And** the button returns to normal state

**Given** I interact with export buttons
**When** I click
**Then** the initial response is within 200ms (NFR4)

**Technical Notes:**

- Wire up onClick handlers in `ActionBar.tsx` for export buttons
- Create `src/components/common/ExportButton.tsx` with loading state
- Call `POST /api/v1/export` with current project state
- Use fetch with blob response handling
- Trigger download using URL.createObjectURL and anchor click
- Show toast notifications for success/error
- Sanitize protocol name for filename

---

### Epic 8 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 8.1 | Implement Export Endpoint with Format Conversion | FR35, FR36, FR37 |
| 8.2 | Generate Approval Tracking Page | FR39 |
| 8.3 | Wire Up Export Buttons and Download | FR35, FR36, FR37, FR38 |

**Total Stories:** 3
**All FRs Covered:** FR35, FR36, FR37, FR38, FR39 ✓
**NFRs Addressed:** NFR4 ✓

**Implementation Notes:**
- Replaced weasyprint with xhtml2pdf (pure Python) — weasyprint requires system C libraries (pango, gobject, cairo) incompatible with Vercel/Render deployment
- Document structure assembled programmatically (title + section headings) — no LLM polishing step needed since LLM already generated section content
- Lazy imports in export_service.py for xhtml2pdf, python-docx, markdown — keeps module loadable even if deps missing, enables clean test mocking
- Approval tracking appended as final section with `---` horizontal rule that renders as page break in DOCX and CSS page-break in PDF
- Timestamp format: "Feb 3, 2026 at 2:30 PM" via `_format_timestamp()`
- Toast notifications: simple state-based toast in DashboardPage, auto-dismiss after 3s, no external library
- Concurrent exports: ActionBar tracks exporting formats in a Set, allows multiple simultaneous exports
- No separate ExportButton component created — export button logic integrated directly into ActionBar

## Epic 9: Multi-Vendor LLM Support

**Goal:** Coordinators can select between LLM vendors (Anthropic, OpenAI) and models; developers can use a local LLM (LM Studio) during development.

---

### Story 9.1: Expand LLM Factory for Multi-Provider Support

As a **developer**,
I want **the backend to support multiple LLM providers (Anthropic, OpenAI, Local)**,
So that **the system can route generation requests to the user's chosen provider**.

**Acceptance Criteria:**

**Given** the server is configured with `LLM_PROVIDER=anthropic` and a valid `ANTHROPIC_API_KEY`
**When** `get_chat_model()` is called with no overrides
**Then** it returns a `ChatAnthropic` instance with the configured model

**Given** `get_chat_model()` is called with `provider="openai"` and `model="gpt-5.1"`
**When** a valid `OPENAI_API_KEY` is configured
**Then** it returns a `ChatOpenAI` instance with model `gpt-5.1`

**Given** `get_chat_model()` is called with `provider="local"`
**When** `ENABLE_LOCAL_LLM=true` and `LOCAL_LLM_BASE_URL` is configured
**Then** it returns a `ChatOpenAI` instance with `base_url` set to the configured URL
**And** model parameter is ignored (Local uses whatever LM Studio is serving)

**Given** `get_chat_model()` is called with `provider="local"`
**When** `ENABLE_LOCAL_LLM=false` or not set
**Then** it raises an error indicating local LLM is not enabled

**Given** `get_chat_model()` is called with `provider="openai"`
**When** no `OPENAI_API_KEY` is configured
**Then** it raises an error indicating the API key is missing

**Given** the config.py Settings class
**When** loaded from environment
**Then** it includes `enable_local_llm: bool = False` and `local_llm_base_url: str = "http://localhost:1234/v1"`
**And** `openai_api_key` comment reflects dual-purpose use (embeddings + LLM)

**Technical Notes:**

- Modify `backend/src/services/llm_factory.py` — expand `get_chat_model()` to accept optional `provider` and `model` parameters
- Three code paths: `"anthropic"` → `ChatAnthropic(model=..., api_key=...)`, `"openai"` → `ChatOpenAI(model=..., api_key=...)`, `"local"` → `ChatOpenAI(model="local", base_url=settings.local_llm_base_url)`
- Modify `backend/src/config.py` — add `enable_local_llm` and `local_llm_base_url` fields
- No new dependencies — `langchain-openai` already installed
- Update `backend/tests/test_llm_factory.py` with tests for all three providers

---

### Story 9.2: Provider Availability Endpoint and Generation Overrides

As a **frontend developer**,
I want **an API endpoint that reports available providers and generation endpoints that accept provider/model overrides**,
So that **the frontend can offer the correct vendor/model choices and route requests accordingly**.

**Acceptance Criteria:**

**Given** the server has `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` configured
**When** I call `GET /api/v1/settings/providers`
**Then** it returns `{ "providers": ["anthropic", "openai"] }`

**Given** the server has `ENABLE_LOCAL_LLM=true`
**When** I call `GET /api/v1/settings/providers`
**Then** the response includes `"local"` in the providers list

**Given** the server has no `OPENAI_API_KEY` configured
**When** I call `GET /api/v1/settings/providers`
**Then** `"openai"` is excluded from the providers list (NFR16)

**Given** I call `POST /api/v1/sections/generate` with `provider: "openai"` and `model: "gpt-5.1"` in the request body
**When** the generation runs
**Then** it uses the specified provider and model instead of server defaults

**Given** I call `POST /api/v1/sections/regenerate` with `provider: "openai"` and `model: "gpt-5.1"` in the request body
**When** the regeneration runs
**Then** it uses the specified provider and model instead of server defaults

**Given** I call a generation endpoint with no `provider` or `model` fields
**When** the generation runs
**Then** it uses the server default provider and model (Anthropic)

**Technical Notes:**

- Create `backend/src/api/routes/settings.py` with `GET /providers` endpoint
- Register settings router in `main.py`
- Check for API key presence (not validity) to determine availability: `settings.anthropic_api_key is not None`, `settings.openai_api_key is not None`
- Check `settings.enable_local_llm` for local provider visibility
- Modify `sections.py` route handlers — add optional `provider` and `model` fields to generate/regenerate request models
- Pass `provider`/`model` through to `section_graph.py` → `section_generator.py` → `llm_factory.get_chat_model()`
- Create `backend/tests/test_settings.py` with provider availability tests

---

### Story 9.3: Frontend Settings UI for Vendor/Model Selection

As a **research coordinator**,
I want **to choose which AI vendor and model to use from the dashboard**,
So that **I can use my preferred provider for ICF generation**.

**Acceptance Criteria:**

**Given** I am on the dashboard page
**When** the page loads
**Then** it fetches available providers from `GET /api/v1/settings/providers`
**And** displays a vendor dropdown with the available options

**Given** I select "OpenAI" from the vendor dropdown
**When** the vendor changes
**Then** the model dropdown updates to show OpenAI models (e.g., gpt-5.1, gpt-5.1-mini-2025-04-14)
**And** the first model is selected by default

**Given** I select "Local" from the vendor dropdown
**When** the vendor changes
**Then** the model dropdown is hidden (FR45)
**And** a note indicates "Uses whatever model LM Studio is serving"

**Given** I select "Anthropic" from the vendor dropdown
**When** the vendor changes
**Then** the model dropdown shows Anthropic models (e.g., claude-sonnet-4-6)

**Given** the vendor/model selection is displayed
**When** no interaction has occurred
**Then** Anthropic is selected as the default vendor (FR46)

**Given** I select a vendor and model
**When** I generate or regenerate a section
**Then** the selected vendor and model are included in the API request body (FR46)

**Given** the settings/providers endpoint returns only `["anthropic"]`
**When** the UI renders
**Then** only Anthropic is shown in the vendor dropdown

**Technical Notes:**

- Add settings UI component to dashboard (e.g., `src/components/dashboard/LlmSettings.tsx`)
- Vendor dropdown populated from `GET /api/v1/settings/providers`
- Model lists are hardcoded per vendor in frontend (no model discovery API):
  - Anthropic: `["claude-sonnet-4-6"]`
  - OpenAI: `["gpt-5.1", "gpt-5.1-mini-2025-04-14"]`
  - Local: no model list (hidden)
- Store selection in React context or component state
- Pass `provider`/`model` to API calls in `src/lib/api.ts` generate/regenerate functions
- Settings should be accessible but not intrusive — consider a collapsible section or settings icon
- Add `fetchProviders()` function to `src/lib/api.ts`
- Add tests in `src/__tests__/` for LlmSettings component and API integration

---

### Story 9.4: Persist Vendor/Model in Project File

As a **research coordinator**,
I want **my vendor and model choice saved in the project file**,
So that **when I reopen a project, it remembers which AI provider I was using**.

**Acceptance Criteria:**

**Given** I have selected OpenAI as the vendor and gpt-5.1 as the model
**When** I save the project
**Then** the project file includes `"llmProvider": "openai"` and `"llmModel": "gpt-5.1"` (FR47)

**Given** I open a saved project that has `llmProvider` and `llmModel` fields
**When** the project loads
**Then** the vendor dropdown is set to the saved provider
**And** the model dropdown is set to the saved model (FR47)

**Given** I open a saved project from before Epic 9 (no `llmProvider`/`llmModel` fields)
**When** the project loads
**Then** the vendor defaults to Anthropic and the model defaults to the server default
**And** no error is shown (backward compatibility)

**Given** a project was saved with `"llmProvider": "local"` and I open it in a deployed environment where Local is not available
**When** the project loads
**Then** the vendor falls back to the default (Anthropic)
**And** a note or warning indicates the original provider is not available

**Given** the project file version
**When** saving with vendor/model fields
**Then** the version is `"1.1"` (updated from `"1.0"`)

**Technical Notes:**

- Update `src/types/project.ts` — add `llmProvider?: string` and `llmModel?: string` to ProjectFile type
- Update `src/lib/projectFile.ts` — include `llmProvider`/`llmModel` in serialize/deserialize
- Update `src/lib/project.tsx` — ProjectProvider state includes vendor/model, loaded from file on open
- Handle backward compatibility: missing fields default to `"anthropic"` / server default model
- Handle unavailable provider on load: check against fetched providers list, fall back if not available
- Bump project file version to `"1.1"`
- Add tests for save/load with vendor/model fields, backward compatibility, and unavailable provider fallback

---

### Epic 9 Summary

| Story | Title | FRs Covered |
|-------|-------|-------------|
| 9.1 | Expand LLM Factory for Multi-Provider Support | FR44 (partial) |
| 9.2 | Provider Availability Endpoint and Generation Overrides | FR44 (partial), FR46 (partial) |
| 9.3 | Frontend Settings UI for Vendor/Model Selection | FR44, FR45, FR46 |
| 9.4 | Persist Vendor/Model in Project File | FR47 |

**Total Stories:** 4
**All FRs Covered:** FR44, FR45, FR46, FR47 ✓
**NFRs Addressed:** NFR16 ✓

**Implementation Notes:**
- No new dependencies — `langchain-openai` already installed; LM Studio reuses ChatOpenAI interface with base_url override
- Anthropic remains the default vendor; existing behavior unchanged when no overrides provided
- Local option visibility controlled by `ENABLE_LOCAL_LLM` env var — only present in dev environments
- Model lists hardcoded in frontend per vendor; no model discovery API needed
- Backward compatible with existing v1.0 project files (missing fields default to Anthropic)
- Stories are sequenced backend-first (9.1 → 9.2) then frontend (9.3 → 9.4) for clean integration
